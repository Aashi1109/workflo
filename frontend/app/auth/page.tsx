import { AuthForm } from "@/components/Auth";
import React from "react";

const page = () => {
  return (
    <div className="flex-center flex-1 h-full bg-gradient-1">
      <AuthForm />
    </div>
  );
};

export default page;
